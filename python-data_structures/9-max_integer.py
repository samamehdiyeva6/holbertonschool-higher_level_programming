#!/usr/bin/python3
def max_integer(my_list=[]):
    if len(max_list) == 0:
        return None
    else:
        max = max_list[0]
        for i in max_list:
            if(max_list[i] > max):
                max = max_list[i]
    return max
